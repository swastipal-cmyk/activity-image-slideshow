name = "activity-votes-results-proxy"
main = "src/index.js"
compatibility_date = "2024-06-01"

# Form Responses spreadsheet (edit if yours differs).
[vars]
SPREADSHEET_ID = "1EFAdD7fzORvoyjDnL2fZ6fCnn36RQKgg74HumpkHva0"
SHEET_RANGE = "Form Responses 1!A:Z"

# Optional: only if you can publish CSV without org restriction (usually not at Agoda).
# SHEET_CSV_URL = "https://docs.google.com/spreadsheets/d/e/.../pub?output=csv"

# Required for Agoda-restricted sheets — set via `wrangler secret put` (see README):
# GOOGLE_SERVICE_ACCOUNT_EMAIL
# GOOGLE_PRIVATE_KEY
